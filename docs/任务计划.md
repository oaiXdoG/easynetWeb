# EasynetWeb 任务计划

## 任务概览

| 序号 | 任务名称 | 状态 | 子任务数 | 优先级 |
|------|----------|------|----------|--------|
| 0 | 菜单同步功能开发 | 未开始 | 8 | 🔥 最高 |
| 1 | 接入后端API，删除所有测试数据 | 未开始 | 6 | 高 |
| 2 | 完善项目初始化流程 | 未开始 | 7 | 高 |
| 3 | 用户创建功能 | 未开始 | 7 | 中 |
| 4 | 角色创建功能 | 未开始 | 7 | 中 |
| 5 | 权限管理功能 | 未开始 | 8 | 中 |
| 6 | groupCode菜单调整 | 未开始 | 6 | 低 |

---

## 零、菜单同步功能开发（优先完成）

### 背景说明

前端 `config/menus.ts` 是菜单配置的唯一数据源，后端数据库需要存储平台菜单数据（`sys_menu_group` 和 `sys_menu` 表）供超管创建项目时选择。因此需要开发一个"同步菜单"功能，让超管可以将前端配置同步到数据库。

**新的表结构设计**（已完成）：
- ✅ `sys_menu_group`：存储菜单分组（console、log、project_settings、system）
- ✅ `sys_menu`：存储具体菜单项（dashboard、log_app、project_member...）
- ✅ `sys_project_menu`：项目-菜单关系表
- ✅ `sys_role_menu`：角色-菜单关系表

详见：`docs/数据库设计.md`

### 子任务清单

| 状态 | 子任务 | 相关文件 | 说明 |
|------|--------|----------|------|
| [√] | 0.0 数据库表结构设计 | `database/schema-new.sql` | 重新设计菜单表结构（双表分离） |
| [√] | 0.0.1 生成初始化数据SQL | `database/seed-menus.sql` | 基于 config/menus.ts 生成初始化数据 |
| [ ] | 0.1 定义同步API类型 | `src/types/menu.ts` | 添加 SyncMenusRequest/Response 类型定义 |
| [ ] | 0.2 添加菜单格式化工具函数 | `src/config/menus.ts` | 将 platformMenus 转换为同步API所需格式（包含 groups 和 menus） |
| [ ] | 0.3 添加同步API接口 | `src/api/system/index.ts` | 新增 syncMenus 函数 |
| [ ] | 0.4 在项目管理页面添加同步按钮 | `src/views/System/Project.vue` | 页面顶部添加"同步菜单"按钮（仅超管可见） |
| [ ] | 0.5 实现同步逻辑 | `src/views/System/Project.vue` | 点击按钮调用API，显示同步结果 |
| [ ] | 0.6 添加加载状态和错误处理 | `src/views/System/Project.vue` | 同步中显示loading，失败显示错误提示 |
| [ ] | 0.7 后端API开发 | 后端 | POST /api/admin/sync-menus 接口实现 |
| [ ] | 0.8 测试同步功能 | - | 测试增删改场景，验证数据库数据正确性 |

### 详细说明

#### 0.1 定义同步API类型

在 `src/types/menu.ts` 中添加：

```typescript
// 同步菜单请求
export interface SyncMenusRequest {
  groups: Array<{
    groupCode: string       // 分组编码
    groupTitle: string      // 分组标题
    sortOrder: number       // 排序
  }>
  menus: Array<{
    groupCode: string       // 所属分组编码
    menuCode: string        // 菜单编码
    menuName: string        // 菜单名称
    icon: string           // 图标
    path: string           // 路由路径
    component: string      // 组件路径
    sortOrder: number      // 排序
  }>
}

// 同步结果
export interface SyncMenusResponse {
  success: boolean
  message: string
  data: {
    groups: {
      added: number
      updated: number
      deleted: number
    }
    menus: {
      added: number
      updated: number
      deleted: number
    }
    total: {
      groups: number
      menus: number
    }
  }
}
```

#### 0.2 添加菜单格式化工具函数

在 `src/config/menus.ts` 中添加：

```typescript
/**
 * 格式化菜单数据用于同步API
 */
export function formatMenusForSync(menus: PlatformMenuGroup[]) {
  const groups: Array<{
    groupCode: string
    groupTitle: string
    sortOrder: number
  }> = []

  const menuItems: Array<{
    groupCode: string
    menuCode: string
    menuName: string
    icon: string
    path: string
    component: string
    sortOrder: number
  }> = []

  for (const group of menus) {
    // 添加分组
    groups.push({
      groupCode: group.groupCode,
      groupTitle: group.groupTitle,
      sortOrder: group.sortOrder
    })

    // 添加该分组下的所有菜单
    for (const item of group.children) {
      menuItems.push({
        groupCode: group.groupCode,
        menuCode: item.menuCode,
        menuName: item.menuName,
        icon: item.icon,
        path: item.path,
        component: item.component,
        sortOrder: item.sortOrder
      })
    }
  }

  return { groups, menus: menuItems }
}
```

#### 0.3 添加同步API接口

在 `src/api/system/index.ts` 中添加：

```typescript
import type { SyncMenusRequest, SyncMenusResponse } from '@/types'

// 同步菜单（分组 + 菜单项）
export function syncMenus(data: SyncMenusRequest) {
  return request.post<SyncMenusResponse>('/admin/sync-menus', data)
}
```

#### 0.4-0.6 前端实现示例

在 `src/views/System/Project.vue` 中：

```vue
<template>
  <div>
    <!-- 同步按钮（仅超管可见） -->
    <button
      v-if="authStore.isSuperAdmin"
      @click="handleSyncMenus"
      :disabled="isSyncing"
    >
      {{ isSyncing ? '同步中...' : '同步菜单' }}
    </button>

    <!-- 项目列表 -->
    <!-- ... -->
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { getAllMenus, formatMenusForSync } from '@/config/menus'
import { syncMenus } from '@/api/system'
import { useAuthStore } from '@/stores'

const authStore = useAuthStore()
const isSyncing = ref(false)

async function handleSyncMenus() {
  try {
    isSyncing.value = true
    const allMenus = getAllMenus()
    const formatted = formatMenusForSync(allMenus)

    const result = await syncMenus(formatted)

    if (result.success) {
      const { groups, menus, total } = result.data
      alert(`同步完成！

分组：新增 ${groups.added}，更新 ${groups.updated}，删除 ${groups.deleted}
菜单：新增 ${menus.added}，更新 ${menus.updated}，删除 ${menus.deleted}

当前总计：${total.groups} 个分组，${total.menus} 个菜单`)
    }
  } catch (error) {
    console.error('同步失败:', error)
    alert('同步失败，请查看控制台')
  } finally {
    isSyncing.value = false
  }
}
</script>
```

#### 0.7 后端API实现要点

后端需要实现 `POST /api/admin/sync-menus` 接口：

1. **权限校验**：验证调用者是超级管理员
2. **同步分组**：
   - 获取当前 `sys_menu_group` 表中的所有分组
   - 对比差异：新增/更新/删除
3. **同步菜单**：
   - 获取当前 `sys_menu` 表中的所有菜单
   - 通过 `groupCode` 找到对应的 `group_id`
   - 对比差异：新增/更新/删除
4. **删除检查**：
   - 删除分组前，检查是否有菜单属于该分组
   - 删除菜单前，检查 `sys_project_menu` 和 `sys_role_menu` 是否有引用
5. **事务执行**：所有变更在一个事务中完成
6. **返回结果**：统计并返回变更数量

**SQL示例**：

```sql
-- 查询需要删除的菜单是否被使用
SELECT COUNT(*) FROM sys_project_menu WHERE menu_id = ?
UNION ALL
SELECT COUNT(*) FROM sys_role_menu WHERE menu_id = ?
```

---

## 一、接入后端API，删除所有测试数据

| 状态 | 子任务 | 相关文件 |
|------|--------|----------|
| [ ] | 1.1 确认后端API基础URL和接口文档 | - |
| [ ] | 1.2 更新baseURL配置 | `src/api/request.ts` |
| [ ] | 1.3 删除mock数据文件 | `src/mock/data.ts` |
| [ ] | 1.4 清理所有引用mock数据的代码 | stores、views等 |
| [ ] | 1.5 更新API接口定义，对接真实后端接口 | `src/api/` |
| [ ] | 1.6 测试API连通性 | - |

---

## 二、完善项目初始化流程

| 状态 | 子任务 | 相关文件 |
|------|--------|----------|
| [ ] | 2.1 完善应用启动逻辑 | `src/main.ts` |
| [ ] | 2.2 优化路由守卫（登录检查、权限验证） | `src/router/guards.ts` |
| [ ] | 2.3 完善认证状态初始化（从后端获取用户信息） | `src/stores/auth/index.ts` |
| [ ] | 2.4 完善权限状态初始化 | `src/stores/permission/index.ts` |
| [ ] | 2.5 完善项目状态初始化 | `src/stores/project/index.ts` |
| [ ] | 2.6 实现token刷新机制 | `src/api/request.ts` |
| [ ] | 2.7 实现登录状态持久化恢复 | `src/stores/auth/index.ts` |

---

## 三、用户创建功能

| 状态 | 子任务 | 相关文件 |
|------|--------|----------|
| [ ] | 3.1 完善用户相关API（CRUD） | `src/api/system/index.ts` |
| [ ] | 3.2 完善用户列表页面 | `src/views/System/User.vue` |
| [ ] | 3.3 实现用户创建表单（用户名、密码、邮箱、手机等） | `src/views/System/User.vue` |
| [ ] | 3.4 实现用户编辑功能 | `src/views/System/User.vue` |
| [ ] | 3.5 实现用户删除功能 | `src/views/System/User.vue` |
| [ ] | 3.6 实现用户状态启用/禁用功能 | `src/views/System/User.vue` |
| [ ] | 3.7 实现用户搜索和分页功能 | `src/views/System/User.vue` |

---

## 四、角色创建功能

| 状态 | 子任务 | 相关文件 |
|------|--------|----------|
| [ ] | 4.1 完善角色相关API（CRUD） | `src/api/project/index.ts` |
| [ ] | 4.2 完善角色列表页面 | `src/views/Project/Role.vue` |
| [ ] | 4.3 实现角色创建表单（角色名、角色编码、描述） | `src/views/Project/Role.vue` |
| [ ] | 4.4 实现角色编辑功能 | `src/views/Project/Role.vue` |
| [ ] | 4.5 实现角色删除功能 | `src/views/Project/Role.vue` |
| [ ] | 4.6 实现角色权限分配功能（关联权限） | `src/views/Project/Role.vue` |
| [ ] | 4.7 实现角色成员查看功能 | `src/views/Project/Role.vue` |

---

## 五、权限管理功能

| 状态 | 子任务 | 相关文件 |
|------|--------|----------|
| [ ] | 5.1 完善权限相关API | `src/api/project/index.ts` |
| [ ] | 5.2 完善权限配置页面 | `src/views/Project/Permission.vue` |
| [ ] | 5.3 实现权限树形展示（菜单权限+操作权限） | `src/views/Project/Permission.vue` |
| [ ] | 5.4 实现权限创建功能 | `src/views/Project/Permission.vue` |
| [ ] | 5.5 实现权限编辑功能 | `src/views/Project/Permission.vue` |
| [ ] | 5.6 实现权限删除功能 | `src/views/Project/Permission.vue` |
| [ ] | 5.7 完善权限校验逻辑 | `src/hooks/permission/index.ts` |
| [ ] | 5.8 完善权限指令 | `src/directives/permission.ts` |

---

## 六、groupCode菜单调整（去掉日志、控制台）

| 状态 | 子任务 | 相关文件 |
|------|--------|----------|
| [ ] | 6.1 移除 'log' 分组 | `src/config/menus.ts` |
| [ ] | 6.2 移除 'console' 分组 | `src/config/menus.ts` |
| [ ] | 6.3 删除或注释日志相关页面 | `src/views/Log/` |
| [ ] | 6.4 更新路由配置，移除相关路由 | `src/router/index.ts` |
| [ ] | 6.5 更新侧边栏菜单渲染逻辑 | `src/components/Layout/AppSidebar.vue` |
| [ ] | 6.6 清理相关类型定义 | `src/types/menu.ts` |

---

## 建议执行顺序

```
第零阶段：菜单同步基础（必须先完成）
└── 任务零：菜单同步功能开发

第一阶段：基础设施
├── 任务一：接入后端API，删除测试数据
└── 任务二：完善项目初始化流程

第二阶段：核心功能
├── 任务三：用户创建
├── 任务四：角色创建
└── 任务五：权限管理

第三阶段：优化调整
└── 任务六：groupCode菜单调整
```

**重要说明：**
- 任务零（菜单同步）是所有功能的基础，必须最先完成
- 只有完成菜单同步后，超管才能创建项目并配置项目菜单
- 后续所有涉及菜单的功能都依赖于此

---

## 待确认事项

- [x] 超管和普通用户的访问规则（已明确）
- [x] 普通用户无项目时的处理（已明确：空布局+提示）
- [x] 角色是项目级的（已明确）
- [x] 菜单同步方案（已明确：选择方案二 - 同步API）
- [ ] 后端API地址（当前配置：`http://localhost:8080`）
- [ ] 后端API文档（Swagger或其他）
- [ ] 除了日志(log)和控制台(console)，是否还有其他需要移除的菜单分组

---

## 更新记录

| 日期 | 更新内容 |
|------|----------|
| 2025-12-24 | 创建任务计划文档 |
